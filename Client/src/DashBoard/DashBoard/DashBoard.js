import React from 'react';
import AppointmentBoard from '../AppointmentBoard/AppointmentBoard';

const DashBoard = () => {
    return (
        <div>
            <AppointmentBoard></AppointmentBoard>
        </div>
    );
};

export default DashBoard;